# Add your MongoDB connection string below. Replace <db_password> with your actual password (URL-encoded if it has symbols).
# MongoDB connection string for Atlas. Replace <db_password> with your password (already filled below).
# We're using database name 'klh_db' for this project.
MONGO_URI=mongodb+srv://anirudhbittu77_db_user:Rt57sVBMR97yax4d@cluster0.addr33a.mongodb.net/klh_db?retryWrites=true&w=majority
# Port the server should listen on
PORT=3001
# JWT secret used to sign authentication tokens. Change to a strong secret in production.
JWT_SECRET=replace-this-with-a-strong-secret
# Google OAuth2 Client ID (used to verify Google ID tokens on the server)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# Allowed email domain for registration (only users with this domain can register)
ALLOWED_EMAIL_DOMAIN=klh.edu.in
# Gmail SMTP credentials for sending OTP emails. Prefer an App Password for Gmail accounts with 2FA.
# Example:
# GMAIL_USER=your-email@gmail.com
# GMAIL_PASS=your-app-password-or-smtp-password

