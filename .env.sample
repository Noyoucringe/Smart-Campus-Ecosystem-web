# Frontend (Vite) environment variables - copy this to .env and fill values
# Public Firebase keys (safe to include in client-side apps)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# Backend API base used by the frontend to call the server
VITE_API_BASE=http://localhost:3001

# Optional: Google OAuth client id (used by Google Identity Services fallback). If you use only
# Firebase signInWithPopup you usually don't need to set this. Create in Google Cloud Console if required.
VITE_GOOGLE_CLIENT_ID=

# NOTES:
# - Do NOT commit your real .env; this sample is safe to commit. Copy to .env and fill values before running the app.